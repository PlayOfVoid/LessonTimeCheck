# Инструкция по развертыванию на PythonAnywhere

## Шаг 1: Подготовка файлов

1. Убедитесь, что все файлы загружены на PythonAnywhere
2. В `settings.py` установите `DEBUG = False` для продакшена

## Шаг 2: Настройка виртуального окружения

1. В консоли PythonAnywhere создайте виртуальное окружение:
   ```bash
   mkvirtualenv --python=/usr/bin/python3.10 learntimecheck
   ```
   (или используйте версию Python, которая доступна)

2. Активируйте окружение:
   ```bash
   workon learntimecheck
   ```

3. Установите зависимости:
   ```bash
   pip install --user -r requirements.txt
   ```

## Шаг 3: Настройка базы данных

1. Выполните миграции:
   ```bash
   python manage.py migrate
   ```

2. Создайте суперпользователя (если нужно):
   ```bash
   python manage.py createsuperuser
   ```

## Шаг 4: Сбор статических файлов

1. Выполните:
   ```bash
   python manage.py collectstatic --noinput
   ```

2. Это создаст папку `staticfiles` с собранными статическими файлами

## Шаг 5: Настройка WSGI файла

1. В панели PythonAnywhere перейдите в раздел **Web**
2. Нажмите на ссылку вашего домена (например, `username.pythonanywhere.com`)
3. Найдите секцию **WSGI configuration file**
4. Откройте файл (обычно `/var/www/username_pythonanywhere_com_wsgi.py`)

5. Замените содержимое на:

```python
import os
import sys

# Добавьте путь к вашему проекту
path = '/home/yourusername/LearnTimeCheck'  # ЗАМЕНИТЕ yourusername на ваш username
if path not in sys.path:
    sys.path.insert(0, path)

# Установите переменные окружения
os.environ['DJANGO_SETTINGS_MODULE'] = 'learn_time_check.settings'

# Импортируем WSGI
from django.core.wsgi import get_wsgi_application
from django.contrib.staticfiles.handlers import StaticFilesHandler

application = StaticFilesHandler(get_wsgi_application())
```

**ВАЖНО:** Замените `yourusername` на ваш username на PythonAnywhere!

## Шаг 6: Настройка статических файлов в панели

1. В разделе **Web** → **Static files**
2. Добавьте следующие пути:

   - **URL:** `/static/`
   - **Directory:** `/home/yourusername/LearnTimeCheck/staticfiles`

   - **URL:** `/static/` (если нужно для статических файлов из папки static)
   - **Directory:** `/home/yourusername/LearnTimeCheck/static`

**ВАЖНО:** Замените `yourusername` на ваш username!

## Шаг 7: Настройка settings.py для PythonAnywhere

Откройте `learn_time_check/settings.py` и убедитесь, что:

```python
DEBUG = False
ALLOWED_HOSTS = ['yourusername.pythonanywhere.com', 'www.yourusername.pythonanywhere.com']
```

**ВАЖНО:** Замените `yourusername` на ваш username!

## Шаг 8: Перезагрузка веб-приложения

1. В панели PythonAnywhere перейдите в раздел **Web**
2. Нажмите зеленую кнопку **Reload** для перезагрузки приложения

## Шаг 9: Проверка работы

1. Откройте ваш сайт: `https://yourusername.pythonanywhere.com`
2. Проверьте:
   - Открывается ли главная страница
   - Работает ли вход в систему
   - Загружаются ли статические файлы (CSS, JS)
   - Работает ли админ-панель: `https://yourusername.pythonanywhere.com/admin/`

## Важные замечания

### Для работы уведомлений Telegram:
- Фоновый поток notifier запускается автоматически при старте Django
- Убедитесь, что у каждого учителя указан `telegram_chat_id` в настройках

### Для обновления кода:
1. Загрузите новые файлы через Files → Upload a file
2. Или используйте Git: `git pull` в консоли
3. Перезагрузите веб-приложение через панель

### Для просмотра логов:
- В панели PythonAnywhere → **Web** → **Error log**
- Там будут видны ошибки, если они возникнут

### Для работы с базой данных:
- SQLite файл находится в `/home/yourusername/LearnTimeCheck/db.sqlite3`
- Можете сделать резервную копию через Files → Download

## Troubleshooting

### Если статические файлы не загружаются:
1. Проверьте пути в разделе **Static files** в панели
2. Убедитесь, что выполнили `collectstatic`
3. Проверьте, что путь указан правильно (с вашим username)

### Если возникает ошибка 500:
1. Проверьте Error log в панели
2. Убедитесь, что `ALLOWED_HOSTS` содержит ваш домен
3. Проверьте, что все зависимости установлены

### Если не работает админ-панель:
1. Убедитесь, что создали суперпользователя
2. Проверьте, что миграции выполнены: `python manage.py migrate`

### Если уведомления не работают:
1. Фоновый поток запускается автоматически
2. Убедитесь, что у учителей указан `telegram_chat_id`
3. Проверьте, что токен бота правильный в `settings.py`

## Полезные команды в консоли PythonAnywhere

```bash
# Активировать окружение
workon learntimecheck

# Перейти в папку проекта
cd ~/LearnTimeCheck

# Обновить зависимости
pip install --user -r requirements.txt

# Выполнить миграции
python manage.py migrate

# Собрать статику
python manage.py collectstatic --noinput

# Создать суперпользователя
python manage.py createsuperuser
```

## Дополнительные настройки

### Для использования собственного домена:
1. В `ALLOWED_HOSTS` добавьте ваш домен
2. В панели PythonAnywhere → **Web** → **Add a new web app**
3. Выберите **Manual configuration**
4. Настройте согласно инструкциям PythonAnywhere

### Для автоматического обновления через Git:
Можно настроить автоматический деплой через Git, но это опционально.

---

**Готово!** Ваш сайт должен работать на PythonAnywhere! 🚀

